@model ShopRosKvartal.Models.Shop.ShopProductsTaste

@{
    ViewBag.Title = "Создание куса товаров";
}

<section class="content">
    <div class="row">

        <div class="col-md-6">
            <div class="box box-primary">
                <div class="box-header with-border text-center">
                    <h3 class="box-title">@ViewBag.Title</h3>
                </div>

                @using (Html.BeginForm("Create", "Tastes", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                {
                    <div class="box-body">
                        @Html.AntiForgeryToken()

                        <div class="form-group">
                            @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "control-label col-md-4" })
                            <div class="col-md-8">
                                @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" } })
                                @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })
                            </div>
                        </div>

                        <div class="box-footer">
                            @Html.ActionLink("Отмена", "Index", "Tastes", null, new { @class = "btn btn-default" })
                            <button type="submit" class="btn btn-primary pull-right">Создать</button>
                        </div>
                    </div>
                }
            </div>
        </div>

    </div>
</section>

@section scripts{
    @Scripts.Render("~/bundles/jqueryval")

    <script>
            $(function () {
                $("#store").addClass("active");
                $("#storetastes").addClass("active");
                $("#storetastescreate").addClass("active");
            });
    </script>
}